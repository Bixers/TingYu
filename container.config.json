{
	"containerPort": 80,
	"minNum": 0,
	"maxNum": 5,
	"cpu": 1,
	"mem": 2,
	"policyType": "cpu",
	"policyThreshold": 50,
	"policyDetails": [
		{
			"PolicyType": "cpu",
			"PolicyThreshold": 50
		},
		{
			"PolicyType": "mem",
			"PolicyThreshold": 50
		}
	],
	"envParams": {},
	"customLogs": "stdout",
	"dataBaseName": "tingyu",
	"executeSQLs": [
		"CREATE DATABASE IF NOT EXISTS tingyu DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;",
		"USE tingyu;",
		"CREATE TABLE IF NOT EXISTS `poems` (`id` VARCHAR(64) PRIMARY KEY, `title` VARCHAR(255) NOT NULL, `dynasty` VARCHAR(50) NOT NULL, `author` VARCHAR(100) NOT NULL, `content` TEXT NOT NULL, `annotation` TEXT, `appreciation` TEXT, `translation` TEXT, `tags` JSON, `source` VARCHAR(100), `source_url` VARCHAR(500), `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP, `updated_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;",
		"CREATE TABLE IF NOT EXISTS `daily_poems` (`id` VARCHAR(64) PRIMARY KEY, `poem_id` VARCHAR(64) NOT NULL, `date` VARCHAR(10) NOT NULL, `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP, UNIQUE KEY `uk_date` (`date`)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;"
	]
}
